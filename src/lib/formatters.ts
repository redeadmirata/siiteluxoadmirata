/**
 * @deprecated Importe diretamente de '@/utils'
 * Este arquivo existe apenas para compatibilidade retroativa.
 * Fonte única de verdade: src/utils/format.ts
 */
export {
  formatCurrency as formatPreco,
  formatArea,
  formatDate as formatData,
  pluralize,
  whatsappUrl,
} from '@/utils'
